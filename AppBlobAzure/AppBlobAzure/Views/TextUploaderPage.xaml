<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppBlobAzure.Views.TextUploaderPage">
    <ContentPage.Content>
        <StackLayout Margin="20,35,20,20">
            <Label 
                Text="Upload Text"
                FontAttributes="Bold"
                HorizontalOptions="Center"/>
            <Label Text="Enter Text"/>
            <Editor
                x:Name="editorTxt"
                HeightRequest="100"/>
            <Button
                x:Name="btnUpload"
                Text="Upload SDK11"
                Clicked="btnUpload_Clicked"/>
            <Button
                x:Name="btnUploadSDK12"
                Text="Upload SDK12"
                Clicked="btnUploadSDK12_Clicked"/>
            <ActivityIndicator
                x:Name="actIndicator"/>
            <Label
                x:Name="lblMessage"
                HorizontalTextAlignment="Center"
                TextColor="Red"/>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>